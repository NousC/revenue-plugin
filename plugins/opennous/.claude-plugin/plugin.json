{
  "name": "opennous",
  "version": "0.19.0",
  "description": "Give Claude one source of truth about your deals, accounts, and activities, with skills that brief you before calls, draft follow-ups, forecast deals and review your pipeline.",
  "author": {
    "name": "OpenNous",
    "url": "https://opennous.cloud"
  },
  "homepage": "https://opennous.cloud",
  "repository": "https://github.com/NousC/revenue-plugin",
  "keywords": ["revenue", "sales", "pipeline", "crm", "forecast", "icp", "gtm", "claude-code", "mcp"],
  "userConfig": {
    "api_key": {
      "type": "string",
      "title": "Nous API key",
      "description": "Run /opennous:login to sign in with your browser (no copy-paste), or paste a key from https://app.opennous.cloud/connect/api-keys. Workspace-scoped.",
      "sensitive": true,
      "required": false
    }
  },
  "mcpServers": {
    "opennous": {
      "command": "npx",
      "args": ["-y", "@opennous/mcp"],
      "env": {
        "NOUS_SURFACE": "plugin",
        "NOUS_API_KEY": "${user_config.api_key}"
      }
    }
  }
}
